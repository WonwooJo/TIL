source('~/.active-rstudio-document', encoding = 'UTF-8')
source('~/.active-rstudio-document', encoding = 'UTF-8')
# 스크립트창 → 파일 저장
# 콘솔창 → 실행결과 확인 / 간단한 코드 실행 결과 확인
print('hello')
# 스크립트창 → 파일 저장
# 콘솔창 → 실행결과 확인 / 간단한 코드 실행 결과 확인
print('hello')
# 스크립트창 → 파일 저장
# 콘솔창 → 실행결과 확인 / 간단한 코드 실행 결과 확인
print('hello')
source('~/.active-rstudio-document', encoding = 'UTF-8')
# 스크립트창 → 파일 저장
# 콘솔창 → 실행결과 확인 / 간단한 코드 실행 결과 확인
print('hello')
save.image("C:/Rwork/Test.RData")
load("C:/Rwork/Test.RData")
savehistory("C:/Rwork/Test.Rhistory")
